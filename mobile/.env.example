# URL of the running FastAPI backend.
# For local dev on a physical device, replace localhost with your machine's LAN IP.
# For iOS Simulator you can keep localhost.
EXPO_PUBLIC_API_URL=http://localhost:8000
